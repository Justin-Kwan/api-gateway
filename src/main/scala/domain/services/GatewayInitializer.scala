package gatewayinitializer

final class GatewayInitializer {

  def initPipelines(): Unit = {
    // reads config json file
    // calls factory to create pipelines
    // where to store the pipeline map during runtime?
  }

}
